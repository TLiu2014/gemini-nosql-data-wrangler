/**
 * Shared display strings whose wording needs to stay consistent across
 * surfaces. Edit once here and every place that links to / refers to the
 * destination updates simultaneously.
 *
 * Add a constant here whenever the same human-readable label appears in
 * more than two places.
 */

/**
 * Label for any button or link whose destination is the main workspace
 * (`/app`). Used in:
 *   - LandingPage header, hero CTA, footer CTA
 *   - DocsPage header, footer CTA
 *   - TopBar (if/when a Launch-app button is added there)
 */
export const LAUNCH_APP_LABEL = "Launch app";
